add -A (all) [Allows you to add the selected files, or all, to the git queue]
commit -m "<MESSAGE>" [Creates a commit that describes the most recent addition of files]
push [Connects to the Internet to send the committed files to the git repo]

git status [Checks the current status]
git clone [Clones the repo to local dir]
